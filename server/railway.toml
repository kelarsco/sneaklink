# Railway deployment for SneakLink backend
# Deploy with Root Directory set to "server" in Railway dashboard

[build]
builder = "nixpacks"

[deploy]
startCommand = "node server.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
